version: "3.8"

services:
  sila2-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "50051:50051"
    volumes:
      - ./results:/app/results
    environment:
      - PYTHONUNBUFFERED=1
